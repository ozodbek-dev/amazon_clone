import { validateNameLength, validatePasswordLength } from "./length.validator"
import { validateEmail } from "./email.validator"
import { validPassowrdMatch } from "./matchPasswords.valdiator"
export {
  validateEmail,
  validatePasswordLength,
  validateNameLength, 
  validPassowrdMatch,
}